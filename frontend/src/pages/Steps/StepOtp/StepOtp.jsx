import React from 'react'

const StepOtp = ({onNext}) => {
  return (
    <div>
      <div>Otp page banega yeh</div>
      <button onClick={onNext}>
        Next 
      </button>
    </div>
  )
}

export default StepOtp
